# Step 7 Closeout — Mark Complete

## Context

Step 7 (`bcm_CapabilityMap` LWC) shipped to `home-denispoc`. Status doc (`2026-05-31-step7-status.md`) recorded 21/21 Playwright passing on 2026-05-31. Between then and 2026-06-05 a further 9+ GH issues landed (#22, #23, #26, #29, #30, #31, #32, #34, #35, #41, #46) extending the diagram with: Detail Panel, cross-cutting band + toggle, Hide From Diagram, sessionStorage map persistence, free vertical pan, focus-outline suppression, context-menu removal, L3 tag colourisation, panel-anchor fix.

`docs/design/99-cosmic-function-point-count.md` was already kept in sync to **119 CFP** (FP29 added, FP30 added, FP31 removed, all UI-only items classed as Excluded).

`docs/plans/implementation-plan.md` tracker still shows step 7 row `[ ]`, total 111. This plan reconciles tracker → FP doc, ticks step 7, and updates the project story.

Latest step-7 commit: `96c005c` 2026-06-05 (`feat(visualisation): colourise L3 bullets by tag`).

## Pre-tick gate

- Trust 2026-05-31 status doc; no e2e/Apex re-runs.
- `force-app/` clean in working tree (only `tokencost/` files dirty).
- Spec markers verified: `grep -nE "not yet covered|UI only" docs/specs/diagram.md` → CLEAN.

## CFP recount

Source-of-truth = `99-cosmic-function-point-count.md`. Current total **119 CFP**. Walk delivered scope against doc:

| Delivered post-original-plan | FP doc treatment | Genuine new FP? |
|---|---|---|
| Detail Panel — open/view (#22) | FP29 — 5 CFP | Yes, already counted |
| Detail Panel — save (#23) | FP30 — 3 CFP | Yes, already counted |
| Hide via Context Menu (#32 removal) | FP31 deleted — −3 CFP | Subsumed by FP30 |
| `bcm_IsCrossCutting__c` field (#29) | FP2 R-widening | No (same data group) |
| CC band rendering (#30) | Exclusion | No (UI re-layout of FP2 payload) |
| CC band toggle (#31) | Exclusion | No (client-side flag) |
| Detail panel anchored to LWC root (#41) | Exclusion | No (CSS) |
| sessionStorage map persistence (#26) | Exclusion | No (Rule 7 Note 2) |
| Focus-outline suppression (#34) | Exclusion | No (CSS) |
| Free vertical pan (#35) | Exclusion | No (CSS / clamp removal) |
| L3 tag colourisation (#46) | **Missing** — needs exclusion entry | No (UI re-render of FP2 payload) |
| `bcm_HideFromDiagram__c` field add | **Missing** — needs exclusion entry | No (same class as `bcm_IsCrossCutting__c`) |
| Keyboard navigation | **Missing** — needs exclusion entry | No (in-memory state) |
| Fit-to-window / Reset view | **Missing** — needs exclusion entry | No (in-memory state) |

**Net delta to 119:** zero. FP29/FP30/FP31 already accounted. Four exclusion-table rows missing; their addition does not change the CFP total.

**Step 7 row CFP** in tracker becomes: FP1 + FP2 + FP3 + FP29 + FP30 = 3 + 5 + 3 + 5 + 3 = **19 CFP** (was 11). Adds FP29/FP30 to the row's roster.

**Project total** = 119 (was 111).

## Edits

### 1. `docs/design/99-cosmic-function-point-count.md`

Append rows to "## 6. Excluded Processes":

| Process | Reason |
|---|---|
| `bcm_HideFromDiagram__c` field add | Same class as `bcm_IsCrossCutting__c` — new boolean on existing `bcm_Capability__c`; widens FP2 selector payload, no new functional process. |
| L3 tag colourisation (GH #46) | Pure SVG re-render of already-loaded `bcm_CapabilityTags__r` data; no new Entry/Exit/Read/Write. Same class as L2 highlight. |
| Keyboard navigation | In-memory focus + pan state on loaded data; no movement crosses boundary. Same class as zoom/pan. |
| Fit-to-window / Reset view | In-memory zoom + pan state mutation; same class as zoom/pan. |

Update delivery-status footnote under summary table to confirm step 7 fully closed at 119 CFP.

### 2. `docs/plans/implementation-plan.md`

**Progress Tracker — step 7 row:**

- `FPs Unlocked` column: change to `FP1–3, FP29–30 (Diagram + Detail Panel)`.
- `CFP` column: `19` (was 11).
- `Cumul.` column: `19+88 = ?` — recompute by walking column. New cumul = sum of new CFPs through step 7. With step 7 = 19, prior cumul through step 6 = 88, so step 7 cumul = **107**.
- `% Done` column: `107 / 119 = 90%`.
- `Status` column: `[x]`.
- `Completed` column: `2026-06-05`.

**Step 8 row:** total stays 8 unchanged from doc, cumul becomes `107 + 12 = 119`, `% Done = 100%`.

**Project totals:** every row's `% Done` may shift slightly (denominator 111 → 119). Recompute each row's percentage off 119.

**Step 7 detail block:**

- Tick all 14 manual-checklist boxes.
- Change `**Step complete:** `[ ]`` → `**Step complete:** `[x]` — deployed to `home-denispoc` 2026-05-30; final scope closed 2026-06-05`.
- Replace existing single `> See also:` line with a block:

```markdown
> See also:
> - [step7-diagram-lwc](2026-05-30-18:30-step7-diagram-lwc.md) — original step plan
> - [step7-adjustments](2026-05-31-06:19-step7-adjustments.md) — visualisation tab variant
> - [step7-visualiser-adjustments](2026-05-31-12:22-step7-visualiser-adjustments.md) — wrap/pin/hide/keyboard
> - [step7-status](2026-05-31-step7-status.md) — execution status, 21/21 e2e pass
> - [capability-detail-panel](2026-06-02-09:48-capability-detail-panel.md) — Detail Panel feature (GH #22/#23)
> - [issue-26 persist selected map](2026-06-03-09:33-issue-26-persist-selected-map.md)
> - [issue-29 isCrossCutting flag](2026-06-03-14:57-issue-29-iscrosscutting-flag.md)
> - [issue-30 cross-cutting band](2026-06-03-16:52-issue-30-cross-cutting-band.md)
> - [cc-band layered fullwidth](2026-06-03-17:30-cc-band-layered-fullwidth.md)
> - [issue-31 cc toggle](2026-06-03-19:41-issue-31-cc-toggle.md)
> - [issue-32 remove context menu](2026-06-03-20:16-issue-32-remove-context-menu.md)
> - [issue-34 suppress canvas focus outline](2026-06-03-12:49-issue-34-suppress-canvas-focus-outline.md)
> - [issue-35 remove svg viewport boundary](2026-06-03-13:52-issue-35-remove-svg-viewport-boundary.md)
> - [issue-41 detail panel clipping](2026-06-04-17:41-issue-41-detail-panel-clipping.md)
> - [l3 tag colourisation](2026-06-05-16:11-l3-tag-colourisation.md)
> - this closeout: [step7-closeout](2026-06-06-11:24-step7-closeout.md)
```

**Adjustments shipped post-plan addendum** — append below the manual-inspection checklist (before "Step complete"):

```markdown
**Adjustments shipped post-plan (closing 2026-06-05):**

- Detail Panel slide-in (GH #22, #23) — replaces context-menu placeholder; FP29 + FP30 in `99-cosmic-function-point-count.md`
- `bcm_IsCrossCutting__c` field + L1 validation (GH #29)
- Cross-cutting band — full-width layered chevron stack at canvas bottom (GH #30)
- Cross-cutting toolbar toggle, hidden-by-default, resets on map switch (GH #31)
- Context menu removed; first-click focus / second-click panel UX (GH #32)
- `bcm_HideFromDiagram__c` field + Show Hidden toggle + dashed-border render
- Keyboard navigation — Arrow keys (pan + node nav), Escape clears focus
- Fit-to-window + Reset view toolbar buttons; zoom/pan reset on map switch
- sessionStorage map-id persistence with stale-id fallback (GH #26)
- Free vertical pan — `panY ≤ 0` clamp removed (GH #35)
- Canvas focus outline suppressed (GH #34)
- Detail panel anchored to LWC root, not canvas container (GH #41)
- L3 bullet tag colourisation — tinted background rect when carrying selected tag (GH #46)
```

### 3. `docs/2026-05-25-project-story.md`

Append new section after "## Where It Stands":

```markdown
---

## Step 7 Closes — 2026-06-05

The diagram landed in stages. The first deploy on 30 May 2026 carried the bones — three Apex controllers, a single LWC rendering chevrons, boxes and bullets, a tag highlight combobox, a context menu stub. By the time the Playwright suite went green at 21/21 on 31 May the read-only slice was demonstrably done.

Then the scope opened. Through early June a sequence of GitHub issues reshaped the diagram from a viewer into a modest editor. The Detail Panel arrived (#22, #23) and absorbed the editing UX that the original plan had left to platform record pages. The context menu — explicitly scoped as a stub for step 7 — was deleted entirely (#32) once the panel made it redundant. A new field, `bcm_IsCrossCutting__c`, surfaced (#29); cross-cutting capabilities pulled out of the column layout into a layered chevron band at the canvas bottom (#30), with a toolbar toggle to show or hide it (#31). A second new field, `bcm_HideFromDiagram__c`, added a way to suppress capabilities from view, with a Show Hidden toggle that revealed them in dashed outline.

Smaller work piled on. Map selection started persisting across reloads via sessionStorage (#26). The vertical-pan clamp came off (#35), the canvas focus outline was suppressed (#34), the detail panel anchored to the LWC root rather than the SVG container so it could not clip on small diagrams (#41). Keyboard navigation appeared. Fit-to-window and Reset view buttons. L3 bullets gained tag colourisation (#46) the day before close.

Through all of it, the COSMIC count held: every UI re-layout, every CSS clamp removal, every in-memory state mutation classified as an excluded process. Only two new functional processes earned new FPs — Detail Panel open (FP29, 5 CFP) and Detail Panel save (FP30, 3 CFP). One was removed (FP31, the context-menu hide path, subsumed into FP30). The total settled at 119 CFP, against the original 111.

Step 7 closed on 5 June 2026. Step 8 — drag-drop — remains.
```

## E2e test updates

None. This closeout is doc-only. No spec changes, no helper changes, no navigation changes. Existing `tests/e2e/diagram.spec.ts` and `force-app/main/default/lwc/bcm_CapabilityMap/__tests__/bcm_CapabilityMap.test.js` remain authoritative.

## File inventory

| Action | Path |
|---|---|
| EDIT | `docs/design/99-cosmic-function-point-count.md` — 4 exclusion rows, footnote tweak |
| EDIT | `docs/plans/implementation-plan.md` — tracker recount, step 7 ticks (block + 14 checkboxes), See also block, post-plan addendum |
| EDIT | `docs/2026-05-25-project-story.md` — append "Step 7 Closes — 2026-06-05" |
| NEW | `docs/plans/2026-06-06-11:24-step7-closeout.md` — this file |

## Build sequence

1. Edit `99-cosmic-function-point-count.md` — add 4 exclusion rows.
2. Edit `implementation-plan.md`:
   a. Step 7 row (FPs Unlocked, CFP=19, Cumul=107, % Done=90%, Status `[x]`, Completed 2026-06-05).
   b. Step 8 row recompute (Cumul=119, %Done=100%).
   c. Recompute earlier rows' `% Done` against denominator 119.
   d. Step 7 detail block: tick 14 boxes, flip closing line, replace `> See also:` with the block, append post-plan addendum.
3. Append story section to `2026-05-25-project-story.md`.
4. `git diff` — review; confirm no `force-app/` touched.
5. Stop. Do not commit (per [no auto-commit](../../.claude/projects/-Users-dkrizanovic-projects-businesscapabilitymap/memory/feedback_no_auto_commit.md)).

## Verification

- `grep -nE "not yet covered|UI only" docs/specs/diagram.md` → empty (already verified).
- `grep -c "\[x\]" docs/plans/implementation-plan.md` increases by 14 (step-7 checklist) + 1 (closing line) + 1 (FP-table row) = 16.
- Project total CFP in implementation-plan.md tracker == `99-cosmic-function-point-count.md` Total Summary == 119.
- All `% Done` columns in implementation-plan.md tracker compute to integers ≤ 100 with denominator 119.
- No metadata XML edited; CLAUDE.md compliance preserved.
